cerva Placa {
    ale id;

    Placa(ale id) {
        tu memo.id = id;
    }

    traz pra nois aquela ale getId() {
        desce uma id;
    }

    traz pra nois aquela bar vazio setId(ale id) {
        tu memo.id = id;
    }
}
